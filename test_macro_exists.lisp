;; Test if lambda macro exists
(print "Testing if lambda macro exists")

;; Try to check if lambda is defined as a macro
;; Since we don't have a direct way to check macro existence, 
;; let's try a very simple lambda usage
(print "Attempting simple lambda usage:")

;; Very simple case
(lambda ())